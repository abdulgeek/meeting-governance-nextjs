# Public URLs of the two backends (NEXT_PUBLIC_ vars are baked at build time).
# Local dev:
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws
NEXT_PUBLIC_API_URL=http://localhost:4000
# On Vercel, set these to the deployed python-api (wss://...) and nest-api (https://...) URLs.
